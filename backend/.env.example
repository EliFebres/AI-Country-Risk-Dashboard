# ──────────────────────────────────────────────────────────────────────────────
# How to use
# 1) Copy this file to `.env` (e.g., backend/.env).
# 2) Fill in the blanks.
# ──────────────────────────────────────────────────────────────────────────────

# === PostgreSQL (Neon) ========================================================
# Format: postgresql://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require
DATABASE_URL=''

# === OpenAI ===================================================================
# Key usually starts with "sk-". Do NOT expose via NEXT_PUBLIC_*.
OPENAI_API_KEY=''

# === Crawlbase ================================================================
# Server-side token for Crawlbase REST API (advanced scraper).
CRAWLBASE_TOKEN=''

# Optional: client-side token for the Crawlbase JS snippet
CRAWLBASE_JS_TOKEN=''

# === FMP (Financial Modeling Prep) ============================================
# API key for the economic-calendar feed (Econ Calendar pane). Plan must
# include the economic-calendar endpoint.
FMP_API_KEY=''
