# ──────────────────────────────────────────────────────────────────────────────
# How to use
# 1) Copy this file to `.env` (e.g., frontend/.env).
# 2) Fill in the blanks.
# ──────────────────────────────────────────────────────────────────────────────

# === PostgreSQL (Neon) ========================================================
# Same Neon database the backend writes to; read server-side by the API routes
# (app/lib/risk-server.ts). NOT exposed to the browser — do NOT prefix NEXT_PUBLIC_.
# Format: postgresql://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require
DATABASE_URL=''
